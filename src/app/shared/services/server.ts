export class Server {
    private baseURL = 'http://localhost:5000';

    private getRoleURl = this.baseURL + '/Role';
    private getCategoryURL = this.baseURL + 'Emp_category';
    private getDepartment = this.baseURL + 'department';
    private getsubCategory = this.baseURL + 'Emp_sub_category';
}